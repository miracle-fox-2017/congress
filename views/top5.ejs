<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Top 5</title>
  </head>
  <body>
    <table border="1">
      <thead>
        <th>Total Vote</th>
        <th>Politician</th>
        <th>Voters</th>
      </thead>
      <tbody>
        <%for (let i = 0; i < candidatesObj.length; i++) { %>
          <tr>
          <td><%=candidatesObj[i].numVotes%></td>
          <td><%=candidatesObj[i].candidate%></td>
          <td><ol><%for (let j = 0; j < candidatesObj[i].voters.length; j++) {%>
            <li><%=candidatesObj[i].voters[j]%></li>
          <%}%></ol></td>
          </tr>
        <%}%>
      </tbody>
    </table>
  </body>
</html>